{% extends '::base.html.twig' %}

{% block body %}
   <h2>Inscription</h2>

	{% for flashMessage in app.session.flashbag.get('notice') %}

	    <div class="bg-succes">{{ flashMessage }}</div>

	{% endfor %}

   {{ form(form) }}

{% endblock %}